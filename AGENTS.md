# Agent notes

Follow `CLAUDE.md`.

1. Read `_ai/00-index.md` before exploring the tree.
2. After you change routes, models, or helpers, run `./scripts/update-brain.sh`.
3. After you change product behaviour, update the matching `_ai/*.md` note in the same change.
4. Never write secrets into `_ai/`.
5. Never hand-edit `_ai/generated/`.
